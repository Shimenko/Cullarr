<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Cullarr" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag "icon.png", type: "image/png" %>
    <%= favicon_link_tag "icon.svg", type: "image/svg+xml" %>
    <%= favicon_link_tag "icon.png", rel: "apple-touch-icon" %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag asset_path("application.css"),
                            asset_path("tailwind.css"),
                            "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="border-b border-slate-200 bg-white">
      <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <div class="flex items-center gap-6">
          <h1 class="text-lg font-semibold text-slate-900">Cullarr</h1>
          <% if operator_signed_in? %>
            <nav class="flex items-center gap-4 text-sm font-medium text-slate-700">
              <%= link_to "Dashboard", dashboard_path, class: "hover:text-slate-900" %>
              <%= link_to "Settings", settings_path, class: "hover:text-slate-900" %>
              <%= link_to "Runs", runs_path, class: "hover:text-slate-900" %>
            </nav>
          <% end %>
        </div>
        <% if operator_signed_in? %>
          <%= button_to "Sign out", session_path, method: :delete,
                        class: "rounded-md border border-slate-300 px-3 py-1.5 text-sm text-slate-700 hover:bg-slate-100" %>
        <% end %>
      </div>
    </header>

    <main class="mx-auto mt-8 w-full max-w-6xl px-6">
      <% flash.each do |key, message| %>
        <% alert_classes = key.to_s == "alert" ? "border-red-300 bg-red-50 text-red-700" : "border-emerald-300 bg-emerald-50 text-emerald-700" %>
        <p class="mb-4 rounded-md border px-4 py-3 text-sm <%= alert_classes %>"><%= message %></p>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
